rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Imagens dos pratos — só admin autenticado pode fazer upload
    match /pratos/{arquivo} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
